let input = "PPPPPP@PPP@PP%P"
let i = 0;
let g = 0;
let out = Infinity; // Initialize out to positive infinity


        // Check if the current count of "P"s is less than the minimum
        // Reset the count

// After the loop, check if the last count is less than the minimum

console.log("Minimum consecutive 'P's count: " + out);